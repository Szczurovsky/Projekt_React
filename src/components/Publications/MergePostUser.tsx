import React, { Component, FC } from "react";
import styled from "styled-components";

interface IMerge {
    userId?: number;
}

export const MergePostUser: FC<IMerge> = () => {
    return <div>{/* <h1>{props.userId}</h1> */}</div>;
};
